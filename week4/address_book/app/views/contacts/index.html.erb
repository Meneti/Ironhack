<h2>List of Contacts</h2>

<ul>
	<% @contacts.each do |contact| %>
	 <a href="/contacts/<%=contact.id%>"><li>
		<%= contact.name%></li></a>
	<% end%>
</ul>

<%= link_to "Create new contact", contacts_new_path %>